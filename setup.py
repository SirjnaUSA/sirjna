from setuptools import setup, find_packages

setup(
    name="sirjna",
    version="0.1.2",
    description="Sirjna — honest, engineering-focused mentorship app (MVP) for Frappe v15",
    author="Parinda LLC",
    author_email="contact@sirjna.com",
    packages=find_packages(),
    include_package_data=True,
    zip_safe=False,
    install_requires=[],
)